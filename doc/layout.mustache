<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=utf-8">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ stylesheet }}">
  <style>
h1 { font-size: 110%; margin-bottom: 6px; }
p { margin-bottom: 4px }
body { background-color: grey; }
div#container { max-width: 940px; margin-left: 1em; background-color: white }
div.docs { margin: 1em 1em 0.2em 1em; padding: 0.5em}
div.code { margin: 0.2em 1em 1em 1em; padding: 0.5em; background-color: #eee }
  </style>
</head>
<body>
<div id='container'>
  {{#sources?}}
  <div id="jump_to">
    Jump To &hellip;
    <div id="jump_wrapper">
      <div id="jump_page">
        {{#sources}}
          <a class="source" href="{{ url }}">{{ basename }}</a>
        {{/sources}}
      </div>
    </div>
  </div>
  {{/sources?}}
  {{#sections}}
  <div id='section-{{ section_id }}'>
    <div class=docs>
      <div class="pilwrap">
        <a class="pilcrow" href="#section-{{ section_id }}">&#182;</a>
      </div>
      {{{ docs }}}
    </div>
    <div class=code>
        <div class='highlight'><pre>{{{ code }}}</pre></div>
    </div>
  </div>
  {{/sections}}
</div>
</body>
